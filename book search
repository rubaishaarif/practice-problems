#include <stdio.h>
struct books{
    char title[10];
    char author[20];
    int price;
    int isbn;
};
void display(struct books book[3]);
void search(struct books *book,int key);
int main() {
   
    int key;
    struct books book[3];
 printf("Enter details of 3 books :");
 for(int i=0;i<3;i++)
 {
     printf("Enter(title, author, price, isbn\n");
     scanf("%s %s %d %d",book[i].title,book[i].author,&book[i].price,&book[i].isbn);
 }
 printf("Enter key(isbn) to search a book : ");
 scanf("%d",&key);
 search(book,key);
 display(book);

    return 0;
}void display(struct books book[3])
{
    printf("\n\t\tDISPLAY\t\t\n");
    for(int i=0;i<3;i++)
    {
        printf("BOOK %d\n",i+1);
        printf("%s\t%s\t%d\t%d",book[i].title,book[i].author,book[i].price,book[i].isbn);
    } }
    void search(struct books *book,int key)
    {
        for(int i=0;i<3;i++)
             if(book[i].isbn == key)
            { printf("match found : %s\n",book[i].title);  return;} 
            else printf("no match found \n");
    }
