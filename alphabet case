#include <stdio.h>

int main() {
char ch;
printf("Enter character : ");
ch = getchar();
if (ch>='A' && ch<='Z')
printf("%c is uppercase \n",ch);
else if (ch>='a' && ch<='z')
printf("%c is lowercase \n",ch);
    return 0;
}
