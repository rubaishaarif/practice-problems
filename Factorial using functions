#include <stdio.h>
int factorial(int n);
int main() {
int num;
printf("Enter number : ");
scanf("%d",&num);
int fact = factorial(num);
printf("%d",fact);
    return 0;
}
int factorial(int n) //3
{
    int i,f=1;
    for(i=n;i>=1;i--)
    {
        f=f*i;
    }
