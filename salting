 //salting
 #include <stdio.h>
 #include<string.h>
void salting(char pass[])
{
    char salt[]="123";
    char newpass[40];
    strcpy(newpass,pass);
    strcat(newpass, salt);
    puts(newpass);
}

int main() 
{
    char password[30];
    scanf("%s",password);
    salting(password);
return 0;
}
