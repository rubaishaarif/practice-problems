#include <stdio.h>

int main() {
char str[20];
char ch;

printf("Enter string : ");
fgets(str,20,stdin);
printf("Enter character : ");
scanf("%c",&ch);
for(int i=0;str[i]!='\0';i++)
{
    if(str[i]!=ch)
    continue;
if(str[i]==ch)
printf("%c is in string",ch);
return 0;

}
printf("%c is not in string",ch);
    return 0;
}
